/**
 * 全局配置
 */
const CONFIG = {
    // API 基础地址
    API_BASE_URL: '/api',
    
    // 本地存储键名
    STORAGE_KEYS: {
        TOKEN: 'library_token',
        ROLE: 'library_role',
        USERNAME: 'library_username'
    },
    
    // 用户角色
    ROLES: {
        ADMIN: 'ADMIN',
        USER: 'USER'
    }
};
